# -*- mode: snippet -*-
# type: command
# name: OutlineExp
# key: outlineexp
# -- Outline Expression
# --
(let ((text (or yas-selected-text ""))
      (beg (point)))
  (insert "\\[\n" text "\n\\]")
  (goto-char (+ beg 3 (length text))))
