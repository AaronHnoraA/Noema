# -*- mode: snippet -*-
# type: command
# name: Math
# key: math
# -- Add a Math
# --
(let ((text (or yas-selected-text ""))
      (beg (point)))
  (insert "\\(" text "\\)")
  (goto-char (+ beg 2 (length text))))
